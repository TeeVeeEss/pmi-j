# Currently max 6 nodes, need to check enumeration
# For each node set ip (ip4, ip6 or dns), port (number), tls (no/yes) and JWT when needed
# NODE_ENV to enable DEV-version with proxy
#NODE_ENV=production
NODE_ENV=development
IRI_NODE_IP_1=localhost
IRI_NODE_PORT_1=14265
IRI_NODE_TLS_1=no
IRI_NODE_JWT_1=